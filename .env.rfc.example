# SAP RFC Connection Configuration
# Copy this file to .env.rfc and fill in your SAP connection details
#
# IMPORTANT: Never commit .env.rfc to version control!
# Add .env.rfc to your .gitignore file.

# ===========================================================================
# Required Settings
# ===========================================================================

# SAP Application Server hostname or IP address
# Example: sap-ecc.company.com or 192.168.1.100
SAP_RFC_ASHOST=sap-ecc.company.com

# SAP System Number (usually 00, 01, or 02)
SAP_RFC_SYSNR=00

# SAP Client (usually 100, 200, 300, or 800)
SAP_RFC_CLIENT=100

# RFC User credentials
# This user needs RFC authorizations (S_RFC) for:
# - Function groups: STXR (READ_TEXT), 2001/2051/2056/2074/2077 (Sales BAPIs)
# - Function modules: SD_SALESDOCUMENT_READ, RFC_READ_TABLE (optional)
SAP_RFC_USER=RFC_USER
SAP_RFC_PASSWD=your_password_here

# ===========================================================================
# Optional Settings
# ===========================================================================

# Login language (default: EN)
SAP_RFC_LANG=EN

# Connection pool size (default: 5, range: 1-50)
# Higher values allow more parallel requests but use more memory
SAP_RFC_POOL_SIZE=5

# RFC trace level (default: 0, range: 0-3)
# 0 = off, 1 = brief, 2 = verbose, 3 = full
# WARNING: Trace files can grow very large at level 2+
SAP_RFC_TRACE=0

# Connection timeout in milliseconds (default: 30000)
# Range: 1000-300000 (1 second to 5 minutes)
SAP_RFC_TIMEOUT=30000

# ===========================================================================
# Docker Compose Settings
# ===========================================================================

# Port for RFC-enabled MCP server (default: 3001)
# The synthetic adapter runs on 3000, RFC adapter on 3001
RFC_SERVER_PORT=3001

# ===========================================================================
# SAP Authorization Requirements
# ===========================================================================
# The RFC user needs these authorization objects:
#
# S_RFC (RFC access)
#   - RFC_TYPE: FUGR
#   - RFC_NAME: STXR, 2001, 2051, 2056, 2074, 2077, 0006, MG01, SUTL
#   - ACTVT: 16 (Execute)
#
# V_VBAK_VKO (Sales Organization)
#   - VKORG: Your sales organizations
#   - ACTVT: 03 (Display)
#
# V_VBAK_AAT (Document Type)
#   - AUART: * (or specific document types)
#   - ACTVT: 03 (Display)
#
# Note: For production, create a dedicated RFC technical user with
# minimal authorizations needed for read-only access.
